@EndUserText.label : 'Expense Policy Rule'
@AbapCatalog.enhancement.category : #NOT_EXTENSIBLE
@AbapCatalog.tableCategory : #TRANSPARENT
@AbapCatalog.deliveryClass : #A
@AbapCatalog.dataMaintenance : #RESTRICTED
define table zexp_policy {

  key client            : abap.clnt not null;
  key policy_uuid       : sysuuid_x16 not null;
  policy_id             : abap.char(10);
  category              : abap.char(10);
  @Semantics.amount.currencyCode : 'zexp_policy.currency'
  max_per_day           : abap.curr(13,2);
  currency              : abap.cuky;
  local_created_by      : abp_creation_user;
  local_created_at      : abp_creation_tstmpl;
  local_last_changed_by : abp_locinst_lastchange_user;
  local_last_changed_at : abp_locinst_lastchange_tstmpl;
  last_changed_at       : abp_lastchange_tstmpl;

}
